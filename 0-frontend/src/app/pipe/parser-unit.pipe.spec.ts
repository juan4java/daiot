import { ParserUnitPipe } from './parser-unit.pipe';

describe('ParserUnitPipe', () => {
  it('create an instance', () => {
    const pipe = new ParserUnitPipe();
    expect(pipe).toBeTruthy();
  });
});
